<?php

namespace Psc\TPL\ContentStream;

/**
 * The EntryEntity does implement this interface already, but only implicit
 * implement this interface in your extending Entry to have the context always set
 */
interface ContextAware {

  // setContext
  // getContext
}
?>